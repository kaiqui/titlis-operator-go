apiVersion: titlis.io/v1
kind: Service
metadata:
  name: titlis-operator-go
spec:
  product: titlis
  team: platform
  tier: tier-1
  description: Operator K8s oficial — extrai snapshots e sincroniza SLOs (Go/controller-runtime).
  workloads:
    - service-sre-titlis-operator-two
